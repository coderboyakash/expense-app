import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const MediaTypePicker = () => {
  return (
    <View>
      <Text>MediaTypePicker</Text>
    </View>
  );
};

export default MediaTypePicker;

const styles = StyleSheet.create({});
